# Phase 14: GT3-049 (R35 GTR) データ投入・展開図・画像編集機能

作業日: 2026-08-31 〜 2026-09-01

## 概要
R35 GTR (GT3-049) のパーツリストを、旧Ver・現行VerのPDF 2種からマージして投入。
全ユニットに展開図を登録し、画像編集（ラインカット）機能を追加した。

## 1. 開発/本番環境の分離
- 開発DB: Docker PostgreSQL 17 (localhost:5437, inventory_dev)
- 本番DB: Neon PostgreSQL 17（従来通り）
- 本番データを開発DBに復元し、開発環境で作業→本番反映のフローを確立
- `.env.local` を開発用に切替。本番設定は `.env.local.backup-production` に退避
- 機密envファイルを `.gitignore` に追加

## 2. ログイン導線の分離・セキュリティ修正
- `/login` = 一般ユーザー用、`/admin/login` = 管理者用に分離
- 運営者が1ログインで管理・ユーザー画面を行き来できるよう ProtectedRoute 調整、
  ダッシュボードに「ユーザー画面を見る」、閲覧画面に「管理画面へ戻る」導線追加
- **重大な脆弱性修正**: `requireAuth` ミドルウェアが管理者判定をしておらず
  一般ユーザーが管理者APIを実行できた問題を修正（userType=admin 検証を追加）

## 3. GT3-049 パーツデータ投入
- 旧Ver (Rev.3.0.11, 421p, 価格あり) × 現行Ver (Rev.4.1, 196p, 価格なし) をマージ
- **マージルール**: 現行PDFにあるパーツのみ登録。パーツ情報は現行、価格は旧Verから
  品番一致で引き継ぎ（品番変更は同位置で引継ぎ、無ければ0円）
- **最終**: 30ジャンル / 177ユニット / **2,471パーツ**、現行PDFと完全一致（取りこぼし・混入・重複0）
- 主な修正: 全角コロン「：」ヘッダ対応、320Sサブユニット分割、表記ゆれ統合と
  別ユニット分離の両立、LOC番号の数値ソート、ジャンル名の誤表示修正、
  パーツ並び順(sortOrder)修正

## 4. 展開図イラスト投入
- 全177ユニットに現行PDFのページ全体を展開図として登録（Cloudinary）
- ページ対応はUNITヘッダとDBユニット名の照合で決定（全件検証、不一致0、重複割当0）

## 5. 画像編集機能（ラインカット）追加
- `ImageEditorDialog` に「ラインでカット（自由曲線トリミング）」モードを追加
- 移動・拡大・背景色に加え、画像をなぞって囲んだ内側だけを残せる
- 管理画面の展開図管理セクションから「画像を編集」ボタンで起動
- 編集後はCloudinaryに保存しDB更新。動作確認済み（保存成功）

## 残タスク
- 価格0円パーツ（約160件、現行の新品番）の手修正（ユーザー作業）
- 各ユニットの展開図のカット編集（ユーザー作業）
- 本番環境への反映
